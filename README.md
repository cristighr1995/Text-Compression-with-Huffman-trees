# Text-Compression-with-Huffman-trees
A text compression application which can compress a text using Huffman technique

# How to run
Compile with "make"

Then write "./huffman -c file_input file_output"
